{
  "name": "{{npmModuleScope}}{{artifactPrefix}}{{artifactName}}{{artifactSuffix}}",
  "version": "{{artifactVersion}}",
  "description": "{{description}}",
  "main": "./dist/index.js",
  "module": "./dist/index.es.js",
  "types": "./dist/index.d.ts",
  "sideEffects": false,
{{#if supportBundling}}
  "browser": "./dist/bundle.js",
  "scripts": {
    "build": "tsc && webpack --config webpack.config.js"
  },
{{else}}
  "scripts": {
    "build": "tsc"
  },
{{/if}}
  "author": {
    "name": "{{generatorName}}",
    "url": "https://github.com/inrupt/lit-artifact-generator-js"
  },
{{#if contributors}}
  "contributors": [
    {{#each contributors}}
    {"name": "{{this}}"}{{#unless @last}},{{/unless}}
    {{/each}}
  ],
{{/if}}
{{#if license}}
  "license": "{{license/name}}",
{{/if}}
{{#if versioning}}
  "repository": {
    "type": "{{versioning.type}}",
    "url": "{{versioning.url}}"
  },
{{/if}}
  "dependencies": {
    "@inrupt/lit-vocab-term": "{{litVocabTermVersion}}",
    "@rdfjs/data-model": "^1.1.2"
  }{{#if supportBundling}},
  "devDependencies": {
    "webpack": "{{webpackVersion}}",
    "webpack-cli": "{{webpackCliVersion}}",
    "ts-loader": "^7.0.0",
    "typescript": "^3.8.3"
  }{{/if}}
}
