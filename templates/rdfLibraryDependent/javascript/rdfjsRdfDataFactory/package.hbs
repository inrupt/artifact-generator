{
  "name": "{{npmModuleScope}}{{artifactPrefix}}{{artifactName}}{{artifactSuffix}}",
  "version": "{{artifactVersion}}",
  "description": "{{{description}}}",
  "main": "index.js",
  "sideEffects": false,
{{#if supportBundling}}
  "scripts": {
    "dev": "webpack --config ./config/webpack.dev.config.js",
    "prod": "webpack --config ./config/webpack.prod.config.js"
  },
{{/if}}
  "author": {
    "name": "{{generatorName}}",
    "url": "https://github.com/inrupt/lit-artifact-generator"
  },
{{#if contributors}}
  "contributors": [
    {{#each contributors}}
    {"name": "{{this}}"}{{#unless @last}},{{/unless}}
    {{/each}}
  ],
{{/if}}
{{#if license}}
  "license": "{{license/name}}",{{/if}}{{#if versioning}}{{#if versioning.url}}
  "repository": {
    "type": "{{versioning.type}}",
    "url": "{{versioning.url}}"
  },{{/if}}{{/if}}
  "dependencies": {
    "rdf-data-factory": "{{rdfjsImplVersion}}"
  }{{#if supportBundling}},
  "devDependencies": {
    "@babel/core": "{{babelCoreVersion}}",
    "babel-loader": "{{babelLoaderVersion}}",
    "webpack": "{{webpackVersion}}",
    "webpack-cli": "{{webpackCliVersion}}"
  }{{/if}}
}
