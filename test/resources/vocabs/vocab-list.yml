#
# This file contains a simple list of vocabularies that we bundle together to
# form the collective set vocabularies within a single artifact.
#
artifact:
  vocabulary:
    inputFiles: [ ./schema.ttl ]
    termSelectFile: schema-inrupt-ext.ttl

  vocabulary:
    inputFiles: [ ./test/resources/vocabs/supported-data-types.ttl ]
